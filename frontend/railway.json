{
  "packages": [
    "java@21.0.2",       // Java for your backend / Maven builds
    "maven@3.9.0",       // Maven if your project uses it
    "nodejs@20.0.0",     // Node.js for npm / frontend build
    "npm"                // Ensure npm is available
  ],
  "commands": [
    "npm install",       // Install frontend dependencies
    "npm run build"      // Build your frontend
  ]
}
